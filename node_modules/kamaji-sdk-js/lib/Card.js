'use strict';
const Core = require('./Core');
/**
 * Card class for request to the customer services with base path /cards
 * @extends Core
 */
class Card extends Core {
    /**
     * @constructor
     */
    constructor(prop) {
        super(prop);
        this._path = '/cards';
        this._token = {};
    }
}
module.exports = Card;