'use strict';
const Core = require('./Core');
/**
 * Customer class for request to the customer services with base path /customers
 * @extends Core
 */
class Customer extends Core {
    /**
     * @constructor
     */
    constructor(prop) {
        super(prop);
        this._path = '/customers';
        this._token = {};
    }
}
module.exports = Customer;