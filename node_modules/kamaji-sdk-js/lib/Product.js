'use strict';
const Core = require('./Core');
/**
 * Product class for request to the customer services with base path /products
 * @extends Core
 */
class Product extends Core {
    /**
     * @constructor
     */
    constructor(prop) {
        super(prop);
        this._path = '/products';
        this._token = {};
    }
}
module.exports = Product;