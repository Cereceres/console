'use strict';
const Core = require('./Core');
/**
 * Order class for request to the customer services with base path /orders
 * @extends Core
 */
class Order extends Core {
    /**
     * @constructor
     */
    constructor(prop) {
        super(prop);
        this._path = '/orders';
        this._token = {};
    }
}
module.exports = Order;