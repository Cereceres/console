'use strict';
const Core = require('./Core');
/**
 * Payment class for request to the customer services with base path /payments
 * @extends Core
 */
class Payment extends Core {
    /**
     * @constructor
     */
    constructor(prop) {
        super(prop);
        this._path = '/payments';
        this._token = {};
    }
}
module.exports = Payment;