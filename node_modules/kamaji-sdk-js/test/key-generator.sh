mkdir test/keys
cd test/keys

# /C=	Country	GB
# /ST=	State	London
# /L=	Location	London
# /O=	Organization	Global Security
# /OU=	Organizational Unit	IT Department
# /CN=	Common Name	example.com

openssl genrsa -out client-key.pem 2048
# For production
#
# openssl req -new -sha256 -key client-key.pem -out client-csr.pem  -subj "/C=MX/ST=DF/L=Condesa/O=4yopping/OU=papi-baby/CN=192.168.15.159"
openssl req -new -sha256 -key client-key.pem -out client-csr.pem  -subj "/C=MX/ST=DF/L=Condesa/O=4yopping/OU=papi-baby/CN=localhost"
openssl x509 -req -in client-csr.pem -signkey client-key.pem -out client-cert.pem
openssl genrsa -out server-key.pem 2048
# openssl req -new -sha256 -key server-key.pem -out server-csr.pem  -subj "/C=MX/ST=DF/L=Condesa/O=4yopping/OU=core/CN=192.168.15.217"
openssl req -new -sha256 -key server-key.pem -out server-csr.pem  -subj "/C=MX/ST=DF/L=Condesa/O=4yopping/OU=core/CN=localhost"
openssl x509 -req -in server-csr.pem -signkey server-key.pem -out server-cert.pem
